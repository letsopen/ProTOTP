<Page
    x:Class="ProTOTP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProTOTP"
    xmlns:converters="using:ProTOTP.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    
    <Page.Resources>
        <converters:StringToColorConverter x:Key="StringToColorConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- 顶部状态栏 -->
            <RowDefinition Height="*"/>  <!-- 主内容区域 -->
            <RowDefinition Height="Auto"/> <!-- 底部添加按钮 -->
        </Grid.RowDefinitions>
        
        <!-- 顶部状态栏 -->
        <Border Grid.Row="0" Background="{ThemeResource SystemControlBackgroundAltHighBrush}" 
                Padding="10" BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" 
                BorderThickness="0,0,0,1">
            <TextBlock x:Name="StatusTextBlock" Text="就绪" HorizontalAlignment="Center" />
        </Border>
        
        <!-- 主内容区域 -->
        <ScrollViewer Grid.Row="1" Padding="0">
            <ListView x:Name="AccountsListView" SelectionMode="None" Margin="0">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}" 
                                BorderThickness="0,0,0,1" Margin="0" Padding="15">
                            <Grid RightTapped="AccountItem_RightTapped" Tag="{Binding}" Tapped="CodeTextBlock_Tapped" 
                            PointerEntered="CodeTextBlock_PointerEntered" PointerExited="CodeTextBlock_PointerExited">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                
                                <TextBlock Grid.Row="0" Text="{Binding AccountName}" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                                <TextBlock Grid.Row="1" Text="{Binding CurrentCode}" FontSize="32" HorizontalAlignment="Center" 
                                           VerticalAlignment="Center" Margin="0,5,0,10" 
                                           Foreground="{ThemeResource SystemControlHighlightAccentBrush}" />
                                <ProgressBar Grid.Row="2" x:Name="TimeProgressBar" Value="{Binding TimeRemainingPercentage}" Maximum="100" 
                                             Height="4" Margin="0,5,0,0">
                                    <ProgressBar.Foreground>
                                        <SolidColorBrush Color="{Binding ProgressBarColor, Converter={StaticResource StringToColorConverter}}" />
                                    </ProgressBar.Foreground>
                                </ProgressBar>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
        
        <!-- 底部添加按钮 -->
        <Border Grid.Row="2" Background="{ThemeResource SystemControlBackgroundAltHighBrush}" 
                BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" 
                BorderThickness="0,1,0,0">
            <Button x:Name="AddAccountButton" Content="添加TOTP账户" Margin="10" 
                    HorizontalAlignment="Stretch" Click="AddAccountButton_Click" 
                    Background="{ThemeResource SystemControlHighlightAccentBrush}" 
                    Foreground="White" Height="40" />
        </Border>
    </Grid>
</Page>
<Page
    x:Class="ProTOTP.Views.AddAccountPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProTOTP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="添加TOTP账户" FontSize="24" Margin="0,0,0,20" HorizontalAlignment="Center"/>
        
        <StackPanel Grid.Row="1" Spacing="10">
            <TextBlock Text="账户名称:" Margin="0,0,0,5"/>
            <TextBox x:ConnectionId='4' x:Name="AccountNameTextBox" PlaceholderText="输入账户名称或服务名称" />
            
            <TextBlock Text="密钥:" Margin="0,15,0,5"/>
            <TextBox x:ConnectionId='5' x:Name="SecretKeyTextBox" PlaceholderText="输入TOTP密钥" TextWrapping="Wrap" AcceptsReturn="True" Height="80"/>
            
            <TextBlock Text="说明 (可选):" Margin="0,15,0,5"/>
            <TextBox x:ConnectionId='6' x:Name="DescriptionTextBox" PlaceholderText="账户的额外说明" TextWrapping="Wrap" Height="60"/>
            
            <TextBlock Text="算法 (可选，默认为SHA1):" Margin="0,15,0,5"/>
            <ComboBox x:ConnectionId='7' x:Name="AlgorithmComboBox" SelectedIndex="0">
                <ComboBoxItem Content="SHA1"/>
                <ComboBoxItem Content="SHA256"/>
                <ComboBoxItem Content="SHA512"/>
            </ComboBox>
            
            <TextBlock Text="时间步长 (可选，默认为30秒):" Margin="0,15,0,5"/>
            <TextBox x:ConnectionId='8' x:Name="TimeStepTextBox" PlaceholderText="时间步长（秒）" Text="30"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="10">
            <Button x:ConnectionId='2' x:Name="SaveButton" Content="保存"                          
                    Background="{ThemeResource SystemControlHighlightAccentBrush}" 
                    Foreground="White" Width="100" Height="40"/>
            <Button x:ConnectionId='3' x:Name="CancelButton" Content="取消"                            
                    Background="Gray" Foreground="White" Width="100" Height="40"/>
        </StackPanel>
    </Grid>
</Page>

